# TODO

- responsive
- add searchbars // besides link ? instead of ? click ? 


![screen](/netstart.png?raw=true "netstart")


bangs to add :
 !gi (Google Images)
 !gt (Google Translate)
 !gmap (Google Map)
 !sx (Stack Exchange)
